<div class="container my-5">
  <h1>Your Cart</h1>
  <hr>
  <% @order.products.each do |product| %>
    <div class="cart-product-card">
      <p><%= product.name %></p>
      <p>Quantity: <%= product.order_items.first.quantity %></p>
    </div>
  <% end %>
</div>
